local M =
{
    _NAME = 'macros-config-utils',
    _DESCRIPTION = [["Barrel" style module combining all relevant (macros directory sub-module) exports for user's macros config.]]
}

local Build = require('macros.build')
local Utils = require('macros.util')
